h2.impact-header Filter Posts
// .ui.left.icon.input
//     i.search.icon
//     +inputAutocomplete settings=settings id="search" class="" autofocus='' autocomplete="off" placeholder='Search Tags'
if selected_post_tags
    // #clear_tags.ui.red.compact.icon.fluid.big.button
    #clear_tags.ui.red.compact.icon.big.button
        i.remove.icon
        // |Clear Filter
if selected_post_tags
    each selected_post_tags
        a.ui.red.compact.big.button.unselect_tag
            strong #{this}
each post_tags
    .ui.compact.basic.large.button.select_tag(class=tag_cloud_class) #{this.name}
        // small #{this.count}
