with service
    .ui.fluid.card
        .content
            +back_button
            .header
                h2.ui.header #{title}
            .description
                // .ui.header service Topics:
                each tags
                    .ui.basic.label #{this}
                |!{description}
        .extra.content
            +favorite
            if isInRole 'admin'
                a.edit_service.ui.primary.basic.button(href="/service/view/#{_id}")
                    i.pencil.icon
                    |Edit Service    