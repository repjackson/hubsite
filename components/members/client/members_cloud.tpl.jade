.ui.center.aligned.basic.segment
    // if isInRole 'admin'
    // if $.Session.get 'checkedin_mode'
    //     #turn_off_checkedin_mode.ui.green.button
    //         i.toggle.on.icon
    //         |Viewing Only Checked In
    // else
    //     #turn_on_checkedin_mode.ui.basic.button
    //         i.toggle.off.icon
    //         |Viewing Only Checked Out
    // .ui.hidden.divider
    .ui.left.icon.input
        i.search.icon
        +inputAutocomplete settings=settings id="search" class="" autocomplete="off" placeholder='Search Tags'

    if selected_member_tags
        #clear_member_tags.ui.red.big.compact.icon.button
            i.remove.icon
    if selected_member_tags
        each selected_member_tags
            a.ui.red.big.compact.button.unselect_member_tag
                strong #{this}
    each all_member_tags
        .ui.compact.basic.button.select_member_tag(class=tag_cloud_class) #{this.name}
            // small #{this.count}
