h2.impact-header Filter
.ui.left.icon.input
    i.search.icon
    +inputAutocomplete settings=settings id="search" class="" autocomplete="off" placeholder='Search Tags'
if selected_eco_tags
    #clear_tags.ui.red.compact.icon.button
        i.remove.icon
if selected_eco_tags
    each selected_eco_tags
        a.ui.red.compact.button.unselect_tag
            strong #{this}
each eco_tags
    .ui.compact.basic.button.select_tag(class=tag_cloud_class) #{this.name}
        small #{this.count}
