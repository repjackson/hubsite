h2.ui.dividing.header.make-uppercase Filter Events
// .ui.left.icon.input
//     i.search.icon
//     +inputAutocomplete settings=settings id="search" class="" autofocus='' autocomplete="off" placeholder='Search Tags'
if selected_event_tags
    #clear_tags.ui.red.compact.icon.labeled.basic.fluid.big.button
        i.remove.icon
        |Clear Filter
if selected_event_tags
    each selected_event_tags
        a.ui.red.compact.big.button.unselect_tag
            strong #{this}
each event_tags
    .ui.compact.basic.large.button.select_tag(class=tag_cloud_class) #{this.name}
        // small #{this.count}
