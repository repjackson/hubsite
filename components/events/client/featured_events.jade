template(name='featured_events')
    .ui.very.padded.attached.segment.container
        h1.ui.header Featured Events
        .ui.four.stackable.cards
            each featured_events
                +featured_event
          
          
          
template(name='featured_event')
    .ui.fluid.card
        .image
            a
                img.ui.medium.image.featured_event_title(src=featured_image_url)
        .content
            .header
                // a(href="/events/view/#{this._id}")
                a
                    h2.ui.header.featured_event_title #{title}
            .description
                // .ui.header Event Topics:
                strong #{day} 
                |from
                strong #{start_time} 
                |to 
                strong #{end_time}
